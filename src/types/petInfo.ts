export type petInfo = {
  name: string;
  image: string;
  bio: string;
  species: string;
  availableForAdoption: boolean;
  noDogs: boolean;
  noCats: boolean;
  noKids: boolean;
  specialNeeds: boolean;
};
